# Agent Control Instructions

This repo's active instructions start in `README.md` and `implement.md`.

If this repo was just cloned or imported into a host project, do not stop after cloning. Read `README.md`, run the adoption protocol, add `agent-control/` to the host `.gitignore` unless the user explicitly wants to vendor it, create or update the host root `AGENTS.md` so future agents read `agent-control/`, inspect the host project, ask the user one optional context question, then bootstrap memory.

Use this file only as a discovery bridge for agent tools that look for `AGENTS.md`; keep detailed runbook and policy doctrine in the files above.
