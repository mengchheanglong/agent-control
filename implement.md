## Agent Control Entrypoint

This repo is the home for the Agent Control workflow surface.

Start with `README.md`, then use `runbook/active.md`, `runbook/current-priority.md`, `policies/stop-lines.md`, `policies/continuation-rules.md`, and `policies/logging-rules.md`.

Keep this file thin and use the other authority surfaces for live guidance.
